import React from "react";
import PropTypes from "prop-types";

const NavigationPage = ({ blogs }) => {
  return <div>NavigationPage</div>;
};

NavigationPage.propTypes = {
  blogs: PropTypes.array.isRequired,
};

export default NavigationPage;
